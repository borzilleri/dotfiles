# Tab Complete ignores case
set completion-ignore-case on
# List all matches when multiple completions exist
set show-all-if-ambiguous on
# If there are >100 matches, ask first before showing them all
set completion-query-items 100
# Do not match hidden files unless pattern starts with dot
set match-hidden-files off
# Add a trailing slash when autocompleting symlinked dirs
set mark-symlinked-directories on
# Show all autocomplete items at once
set page-completions off
# Show extra file info when completing, like ls -F
set visible-stats on
# Allow UTF-8 input/output
set input-meta on
set output-meta on
set convert-meta off

